<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Nicely Designed Video Popup</title>
<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f0f0f0;
    }
    .video-container {
        display: none;
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background-color: rgb(255, 255, 255);
        border-radius: 8px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3);
        overflow: hidden;
        z-index: 1000;
    }
    .video-content {
        position: relative;
        padding: 20px;
    }
    .video-content video {
        width: 100%;
        height: auto;
        display: block;
    }
    .close-button {
       padding: 15px 25px;
        position: absolute;
        top: 6px;
        right: 6px;
        cursor: pointer;
        color: rgb(255, 0, 0);
        font-size: 24px;
    }
    .overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
        z-index: 999;
    }
     body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #e7e7e7;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
    }
    .popup-button:hover {
        background-color: #0056b3;
    }
</style>

</head>
<body>


<script>
    function showVideoPopup() {
        // Your code to show the video popup here
        alert('Showing video popup!');
    }
</script>


<div class="overlay" id="overlay"></div>

<div class="video-container" id="videoContainer">
    <div class="video-content">
        <span class="close-button" onclick="closeVideoPopup()">&times;</span>
        <video controls autoplay>
            <source src="your-video-url.mp4" type="video/mp4">
            Your browser does not support the video tag.
        </video>
    </div>
</div>

<button onclick="showVideoPopup()">Show Video Popup</button>

<script>
    function showVideoPopup() {
        var overlay = document.getElementById('overlay');
        var videoContainer = document.getElementById('videoContainer');
        overlay.style.display = 'block';
        videoContainer.style.display = 'block';
    }

    function closeVideoPopup() {
        var overlay = document.getElementById('overlay');
        var videoContainer = document.getElementById('videoContainer');
        overlay.style.display = 'none';
        videoContainer.style.display = 'none';
    }
</script>

</body>
</html>
